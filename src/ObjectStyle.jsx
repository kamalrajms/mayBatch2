import React from "react";

export default function ObjectStyle() {
  const theme = {
    padding: "20px",
    backgroundColor: "blue",
    color: "#fff",
  };
  return <div style={theme}>object style</div>;
}
